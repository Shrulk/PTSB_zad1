#include "iostream"
#include <array>

using namespace std;

double F(double x) {
    return x;
}

int main() {
    double p; // Каппа в нашей задаче, > 0
    double q; // > 0
    double x = 0, y = 0; // Координаты
    array<double, 2> u = {x, y};

    return 0;
};